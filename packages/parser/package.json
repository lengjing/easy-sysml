{
  "name": "@easy-sysml/parser",
  "version": "0.1.0",
  "type": "module",
  "license": "LGPL-3.0-or-later",
  "description": "SysML v2 / KerML parser built on Langium",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "langium:generate": "langium generate && node scripts/fix-generated.cjs",
    "build": "npm run langium:generate && tsc --build",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "langium": "~3.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "langium-cli": "~3.3.0",
    "typescript": "~5.8.3",
    "vitest": "^3.1.3"
  }
}
