{
  "name": "easy-sysml",
  "version": "0.1.0",
  "private": true,
  "description": "Next-generation SysML v2 language tooling platform",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean",
    "dev:webapp": "pnpm --filter @easy-sysml/webapp dev",
    "dev:extension": "pnpm --filter @easy-sysml/extension build"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.12.0"
  }
}
