{
  "name": "easy-sysml",
  "version": "0.1.0",
  "private": true,
  "description": "SysML v2 Language Tooling Platform",
  "license": "LGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "clean": "pnpm -r run clean"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  }
}
